import useTitle from '../../hooks/useTitle';
import './blog.css'

const Blog = () => {
    useTitle('Blog')
    return (
        <div>
            <h1>Blog</h1>
        </div>
    );
};

export default Blog;